import 'package:flutter/material.dart';

class ListagemDashBoard extends StatelessWidget {
  const ListagemDashBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
